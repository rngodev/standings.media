services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: takeoff
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
